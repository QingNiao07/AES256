# AES-256 加密工具箱 · 使用说明书

版本 v1.2 ｜ 作者：青鸟（Qingniao）`<qingniao2007@126.com>`

本软件是一款**本地离线**的数据加密工具箱，用于对文件、文件夹进行加密、解密与
完整性校验。默认不联网，密钥与明文均不出本机。

> ⚠ 使用前请先阅读并接受《法律声明与免责协议》（`docs/LEGAL.md`）。
> 只有本人勾选"我接受"后，才能完成安装或首次使用。

---

## 一、安装

| 方式 | 命令 | 说明 |
|------|------|------|
| 图形向导（推荐） | `python install.py` | 阅读声明 → 勾选"我接受" → 完成安装 |
| 命令行 | `python install.py --cli` | 需输入"我接受" |
| 无界面 | `python install.py --quiet --accept --who 名字` | 自动化；必须显式 `--accept` |
| 仅检查环境 | `python install.py --check` | 检查依赖与法务状态，不安装 |

安装动作：初始化配置、创建日志目录 `aes_log/`、记录法务接受信息（`config.json`
的 `legal` 段 + 审计日志）。**未勾选"我接受"时安装会被终止，不写入任何接受记录。**

---

## 二、启动

| 前端 | 命令 | 说明 |
|------|------|------|
| PySide6（推荐） | `python aes256_qt.py` | 功能最全，界面现代化 |
| Tkinter（轻量） | `python aes256_tool.py` | 无额外依赖 |
| 命令行 | `python cli.py --help` | 批处理 / 自动化 |

首次启动会弹出《法律声明与免责协议》，勾选"我接受"后才能进入。

---

## 三、三步开始使用

1. **输入密码** → 点【缓存密码】。
2. **把文件拖进窗口** → 自动加密，生成 `.aes256` 文件。
3. **把 `.aes256` 文件拖进窗口** → 自动解密还原。

### 两条铁律
- **永远不要删除密钥文件。**
- **首次使用不要勾选"删除原文件"**，确认整个加解密流程无误后再启用。

### 立即做
点【导出密钥文件】保存到 U 盘，并**抄写一份恢复码**（抗介质损坏）。

---

## 四、多加密方式与叠加加密

- 菜单【文件 → 选择加密方式…】，或命令行 `python cli.py list-ciphers`。
- 可**单选**一种算法，也可勾选多种**叠加**（自上而下依次加密）。
- **无论叠加几种算法，始终只使用一个总密钥**：密码只派生一次，各层子密钥由
  HKDF 域分离得到。

支持算法：AES-256-GCM、AES-256-OCB3、ChaCha20-Poly1305、AES-256-CBC+HMAC、
Camellia-256+HMAC、CAST5、Blowfish、Triple-DES。

```bash
python cli.py enc 文件 --chain aes-256-gcm
python cli.py enc 文件 --chain aes-256-gcm,chacha20-poly1305
python cli.py dec 文件.aes256        # 解密自动识别算法链
```

详见 `docs/MULTI_CIPHER.md`。

---

## 五、中英双语

菜单【语言 / Language】可运行时切换，配置持久化（`config.json` 的 `ui.language`）。

---

## 六、离线安全与 AI 安全限制

- **默认离线**：网络守卫拦截一切对外网络连接。
- **AI 助手默认离线**，需在界面显式勾选"允许联网"才会联网。
- **AI 安全限制**：提示注入拦截、敏感内容拦截（私钥/密钥/长十六进制等）、会话
  额度与频率限制、出网前脱敏、系统提示词硬约束。
- AI 提问内容不写入操作日志，仅记录元数据到 `ai_audit.log`。

---

## 七、法律与免责

- 完整条款见 `docs/LEGAL.md`，或菜单【帮助 → 法律声明与免责协议】。
- **合法用途**：仅用于合法目的，不用于危害国家安全、损害公共利益或侵害他人权益的活动。
- **用途须符合社会主义核心价值观**。
- **责任划分**：软件是"工具"，使用者是"操作者"；使用者的使用行为及其后果由使用者
  自负，与作者无关。
- 作者按"现状"提供软件，不作担保；因使用或无法使用本软件导致的损失，作者不承担责任。
- 密码与密钥文件同时丢失无法恢复，请务必妥善保管。

---

## 八、快捷键

| 快捷键 | 功能 |
|--------|------|
| F1 | 使用说明 |
| Ctrl+Shift+C | 选择加密方式 |
| Ctrl+Shift+P | 命令面板 |
| Ctrl+Shift+A | AI 助手 |
| Ctrl+Shift+T | 切换主题 |
| Ctrl+Shift+E | 扩展工具 |
| Ctrl+L | 立即锁定 |

---

## 九、常见问题

**Q：忘记密码怎么办？**
A：加密不可逆，密码无法找回。若提前导出了密钥文件，可用它解密；否则数据无法恢复。

**Q：为什么叠加加密时也只有一个密码？**
A：这是设计约束——密码只派生一次总密钥，各层子密钥由 HKDF 域分离得到，所以你只需
记住一个密码。

**Q：程序会联网吗？**
A：默认不会。网络守卫会拦截对外连接；AI 助手需你显式授权才会联网。

**Q：日志在哪里？**
A：程序目录 `aes_log/`：`operation.log`（操作）、`security.log`（安全）、
`ai_audit.log`（AI 元数据）。
